backend:
  name: git-gateway
  branch: main

media_folder: "gambar"
public_folder: "/gambar"

collections:
  - name: "portfolio"
    label: "Portfolio"
    folder: "content"
    create: true
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}
